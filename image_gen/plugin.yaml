name: minimax
version: 1.0.0
description: "MiniMax image generation backend (image-01). Text-to-image and single-reference image generation."
author: local
kind: backend
requires_env:
  - MINIMAX_API_KEY
